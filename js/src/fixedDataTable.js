define(['react', 'fixedDataTable'], function (React, FixedDataTable) {
    var Table = FixedDataTable.Table;
    var Column = FixedDataTable.Column;
    var FixedDataTable = React.createClass({

        render: function () {
            return (
                <div></div>
            )
        }
    });

    return FixedDataTable;
})